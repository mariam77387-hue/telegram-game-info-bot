services:
  - type: worker
    name: telegram-game-info-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend.py
    envVars:
      - key: BOT_TOKEN
        sync: false